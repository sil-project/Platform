{% block flash_message %}

    {% set icons = {
        'success': 'check circle',
        'error': 'times circle',
        'warning': 'exclamation circle',
        'notice': 'lightbulb',
        'info': 'info circle'
    } %}

    {% if label is not defined %}
        {% set label = '%%type%%' %}
        {% set icon = 'lightbulb' %}
    {% else %}
        {% set icon = icons[label] %}
    {% endif %}

    {% if message is not defined %}
        {% set message = '%%message%%' %}
    {% endif %}

    <div class="ui compact {{ label }} message" data-icons="{{ icons|json_encode|e('html_attr') }}">
        <i class="close icon"></i>
        <div class="content">
            <i class="{{ icon }} icon"></i>
            <p>
                {{ message|trans|raw }}
            </p>
        </div>
    </div>
{% endblock flash_message %}
