<p style="color: #EEE">
    {% if object.isIndividual %}
        {{ 'sil.seed_batch.seed_producer.show.label.individual'|trans() }}:
        <b>{{ object.firstName }} {{ object.lastName }}</b>
    {% else %}
        {{ 'sil.seed_batch.seed_producer.show.label.organization'|trans() }}
        <b>{{ object.name }}</b>
    {% endif %}

    {% if object.customerCode is not null and object.customerCode != '' %}
        &nbsp;|&nbsp;{{'sil.seed_batch.seed_producer.show.label.customer_code'|trans}}: <b>{{object.customerCode}}</b>
    {% endif %}
    {% if object.supplierCode is not null and object.supplierCode != '' %}
        &nbsp;|&nbsp;{{'sil.seed_batch.seed_producer.show.label.supplier_code'|trans}}: <b>{{object.supplierCode}}</b>
    {% endif %}
    {% if object.seedproducercode is not null and object.seedproducercode != '' %}
        &nbsp;|&nbsp;{{'sil.seed_batch.seed_producer.show.label.seed_producer_code'|trans}}: <b>{{object.seedproducercode}}</b>
    {% endif %}
</p>
